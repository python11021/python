# 함수의 정의
# 함수의 타입(4가지 경우)
    # 매개변수 O, 반환값 O
    # 매개변수 O, 반환값 X
    # 매개변수 X, 반환값 O
    # 매개변수 X, 반환값 X
# 매개변수 종류
    # 기본 매개변수 :뒤에서부터 채운다
    # 가변 매개변수 : 함수 내부에서 리스트로 저장 * 키워드 사용
    # 키워드 매개변수   -> 딕셔너리
    # 가변 키워드 매개변수

# 함수 생성
    # def 함수명(옵션):
        # 내용 필수
        # return 은 옵션
# 함수호출
    # 함수이름(옵션)
    # 함수는 호출이 끝나면 해당 위치에 반환값이 있으면 가지고 온다.
    # result = myFunc()